services:
  base-runtime:
    build:
      context: ../../../..
      dockerfile: infra/images/base.Dockerfile
    image: orchestrator-base:local
    command: ["true"]

  worker:
    build:
      context: ../../../..
      dockerfile: infra/images/worker.Dockerfile
    image: orchestrator-worker:local
    container_name: ${WORKER_CONTAINER_NAME:-orchestrator-worker-fixed-cpu-test}
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 8.8.8.8
    environment:
      PREFECT_API_URL: ${PREFECT_API_URL}
      PREFECT_WORK_POOL: ${PREFECT_WORK_POOL:-cpu-test-pool}
      PREFECT_WORK_QUEUE: ${PREFECT_WORK_QUEUE:-cpu-fixed-test}
      PREFECT_CLIENT_CUSTOM_HEADERS: ${PREFECT_CLIENT_CUSTOM_HEADERS:-}
      CF_ACCESS_CLIENT_ID: ${CF_ACCESS_CLIENT_ID:-}
      CF_ACCESS_CLIENT_SECRET: ${CF_ACCESS_CLIENT_SECRET:-}
      PREFECT_BROWSER_USER_AGENT: ${PREFECT_BROWSER_USER_AGENT:-}
      STORAGE_API_URL: ${STORAGE_API_URL:-}
      ORCHESTRATOR_CHECKPOINT_DIR: /var/lib/orchestrator/checkpoints
    volumes:
      - ${WORKER_CHECKPOINT_DIR:-/tmp/orchestrator-worker-cpu-test-checkpoints}:/var/lib/orchestrator/checkpoints
